import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const MapPage = ({elem}) => {
  return (
      <View> 
        <Text> current Mapss page: {elem}</Text> 
      </View>
    )
}

export const ArtPage = ({elem}) => {
  return (
      <View> 
        <Text> current Art page: {elem}</Text> 
      </View>
    )
}

export const CommunityPage = ({elem}) => {
  return (
      <View> 
        <Text> current CommunityPage page: {elem}</Text> 
      </View>
    )
}

export const ArtistsPage = ({elem}) => {
  return (
      <View> 
        <Text> current ArtistsPage page: {elem}</Text> 
      </View>
    )
}